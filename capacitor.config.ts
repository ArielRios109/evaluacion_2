import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.evaluacion_1.app',
  appName: 'y',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
